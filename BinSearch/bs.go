package bsearch

import "log"

func init() {
	log.SetFlags(0)
	log.Print("> binSearch")
}

// 2300
func SuccessfulPairs(spells []int, potions []int, success int64) []int {
	pairs := make([]int, len(spells))
	return pairs
}
